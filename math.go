package main

type wxlAdd struct {
}

// func (w wxlAdd) Execute(execution wxlExecution) wxlMethodResult {
// 	var rToken = LToken{LT_NUMBER, "0", 0}

// 	for i, token := range execution.parameters {
// 		if i > 0 && token.kind == LT_NUMBER {
// 			rToken.numberValue = rToken.numberValue + token.numberValue
// 		}
// 	}

// 	return rToken
// }
